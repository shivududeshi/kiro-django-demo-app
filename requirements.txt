# ============================================================
# requirements.txt — Python 3.12 compatible
# Upgraded from Python 3.7.2 / Django 1.11
# ============================================================

# Core framework (upgraded from 1.11.29 — EOL, incompatible with Py 3.12)
Django==4.2.13

# Production WSGI server (replaces runserver)
gunicorn==22.0.0

# Environment variable loading from .env file
python-dotenv==1.0.1

# Database driver
# mysqlclient requires libmysqlclient-dev system headers — install with:
#   sudo apt install python3-dev default-libmysqlclient-dev build-essential
# Uncomment below once headers are available:
# mysqlclient==2.2.4

# PyMySQL: pure-Python MySQL driver, no system headers needed
PyMySQL==1.1.1

# Search (upgraded from 2.8.1 — incompatible with Django 4.x)
django-haystack==3.3.0
Whoosh==2.7.4

# Phone number field (upgraded from 2.2.0 — incompatible with Django 4.x)
django-phonenumber-field==7.3.0
phonenumbers==8.13.39

# Cryptography (upgraded from 2.5 — won't build on Py 3.12)
cryptography==42.0.8

# HTTP / networking
requests==2.32.3
urllib3==2.2.2
certifi==2024.6.2
idna==3.7
chardet==5.2.0

# Auth
oauthlib==3.2.2
requests-oauthlib==2.0.0
PyJWT==2.8.0

# Date/time
python-dateutil==2.9.0.post0
pytz==2024.1

# XML / spreadsheet (used for data loading utilities)
lxml==5.2.2
openpyxl==3.1.5

# REMOVED packages (reasons below):
# enum34       — Python 2 backport; breaks on Python 3.12 (stdlib enum is used)
# six          — Python 2/3 compat shim; not needed in Python 3.12
# future       — Python 2/3 compat shim; not needed in Python 3.12
# configparser — Python 2 backport; stdlib in Python 3
# pyOpenSSL    — not directly imported by this project
# PySocks      — not directly imported by this project
# asn1crypto   — pulled in transitively by cryptography if needed
# cffi         — pulled in transitively by cryptography if needed
# pycparser    — pulled in transitively by cffi if needed
# Babel        — not used in this project
# protobuf     — not used in this project
# et-xmlfile   — pulled in transitively by openpyxl
# jdcal        — pulled in transitively by openpyxl (older versions)
# xlrd         — not used in this project (openpyxl handles xlsx)
